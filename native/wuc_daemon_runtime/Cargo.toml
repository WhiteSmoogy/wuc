[package]
name = "wuc_daemon_runtime"
version = "0.1.0"
edition = "2021"

[lib]
name = "wuc_daemon_runtime"
crate-type = ["cdylib"]

[dependencies]
once_cell = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
